// NOT IN USE
/*   
   var appLayout = [
      "app/controllers",
      "app/helpers",
      "app/models",
      "app/views/layouts",
      "config/environments",
      "db",
      "doc",
      "generators",
      "i18n",
      "log",
      "modules",
      "public",
      "script",
      "test/mocks/environment",
      "tmp",
      "vendor/plugins"
   ];
   var APP_DIR = new File(APP_NAME);
   var d;
   for (var i=0; i<appLayout.length; i++) {
      d = new File(APP_DIR + "/" + appLayout[i]);
      if (!d.exists()) {
         shell.writeln("create directory " + d);
         res.writeln("create directory " + d);
         d.makeDirectory();
      }
   }
*/
